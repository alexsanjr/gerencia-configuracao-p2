# Copie para postgresql-secret.yaml e substitua os valores antes de aplicar.
# O arquivo postgresql-secret.yaml deve permanecer fora do Git.
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-replication
  namespace: voce-aluga
type: Opaque
stringData:
  postgres-password: PREENCHA_AQUI
  application-password: PREENCHA_AQUI
  replication-password: PREENCHA_AQUI
