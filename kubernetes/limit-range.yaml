apiVersion: v1
kind: LimitRange
metadata:
  name: voce-aluga-limits
  namespace: voce-aluga
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      default:
        cpu: 500m
        memory: 512Mi
